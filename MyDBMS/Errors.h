#pragma once

#include <string>

using namespace std;

const static string ERROR_DB_NAME_OVERLENGTH = "invalid database name: the length of database name must be less than 128 charactors.";
const static string ERROR_DB_NOT_EXIST = "the database is not exist.";